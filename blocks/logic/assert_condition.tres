[gd_resource type="Resource" load_steps=2 format=3]

[ext_resource type="Script" path="res://addons/block_code/code_generation/block_definition.gd" id="1_assert_condition"]

[resource]
script = ExtResource("1_assert_condition")
name = &"assert_condition"
description = "Stop with an error in debug builds if the condition is false."
category = "Logic | Boolean"
type = 2
variant_type = 0
display_template = "assert {condition: BOOL}"
code_template = "assert({condition})"
defaults = {
"condition": true
}
