[gd_resource type="Resource" load_steps=2 format=3]

[ext_resource type="Script" path="res://addons/block_code/code_generation/block_definition.gd" id="1_string_equals"]

[resource]
script = ExtResource("1_string_equals")
name = &"string_equals"
description = "True if two strings are equal."
category = "Logic | Comparison"
type = 3
variant_type = 1
display_template = "{a: STRING} equals {b: STRING}"
code_template = "{a} == {b}"
defaults = {
"a": "A",
"b": "B"
}
