[gd_resource type="Resource" load_steps=2 format=3]

[ext_resource type="Script" path="res://addons/block_code/code_generation/block_definition.gd" id="1_objects_same"]

[resource]
script = ExtResource("1_objects_same")
name = &"objects_same"
description = "True if two objects are the same instance."
category = "Logic | Comparison"
type = 3
variant_type = 1
display_template = "{a: OBJECT} is same as {b: OBJECT}"
code_template = "{a} == {b}"
defaults = {}
